specVersion: "v1beta"
serviceId: "3. GameBrogemon"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "3. GameBrogemon-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
